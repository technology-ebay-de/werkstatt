import { combineReducers } from '@modular-toolkit/bricks'

export default combineReducers({
  page (state = {}, action = {}) {
    switch (action.type) {
      default:
        return state
    }
  }
})
